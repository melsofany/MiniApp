أريد منك إنشاء نظام متكامل يتكون من:
(1) بوت تيليجرام
(2) تطبيق مصغّر Mini App
(3) داشبورد ويب بكلمة مرور
(4) Google Sheets
(5) معالجة صورة البطاقة بالذكاء الاصطناعي
(6) دعم 500 مستخدم متزامنين
(7) يعمل على Render Free بدون توقف

🔐 أولًا: الداشبورد (Dashboard)

صفحة تسجيل دخول بكلمة مرور:
Elec@alaa2025

بعد الدخول:

عرض المستخدمين المسموح لهم باستخدام البوت

عرض إحصائيات العمليات

زر إضافة UserID

زر حذف UserID

سجل البطاقات التي تم إدخالها

رسم بياني لعدد العمليات اليومية

🤖 ثانيًا: بوت تيليجرام

التوكن:
8344279110:AAEKsf4iMeNbiElEEQA4qdPkzp5oJhthwpA

البوت يفتح التطبيق المصغّر مباشرة بعد التحقق من USER_ID.

📱 ثالثًا: التطبيق المصغّر (Mini App)

عند فتح التطبيق:

يطلب تصوير البطاقة الشخصية (الوجه الأمامي).

يتم تلقائيًا:

🔧 المعالجة بالذكاء الاصطناعي:

إصلاح اتجاه الصورة Auto-Rotate

تعديل الصورة لو كانت مقلوبة 180°

تعديل الميل Tilt Fix

رفع الجودة

قص الجزء الخاص بالبطاقة فقط Crop

تحسين الإضاءة Auto Enhance

استخراج:

الاسم بالكامل

الرقم القومي

حفظ البيانات في Google Sheets خلال 5 ثواني.

إن كانت البطاقة مكررة:

يتم منع الإدخال

ويظهر اسم المستخدم + UserID الذي أدخلها سابقًا.

🧠 رابعًا: الذكاء الاصطناعي

استخدم Google AI Studio عبر API Key:
AIzaSyBR0Xr6nAcByETSOO13oK-KyDr3udwmtCo

المهام:

Auto fix image

OCR

استخراج البيانات بدقة عالية وفي أقل من 5 ثواني.

📄 خامسًا: Google Sheets

لا تُستخدم قواعد بيانات. فقط Google Sheets.

الملف ID:

15hjsS5SvoZP2Qlt4tBSnczR40yZcn3HD31lpqU_BtVs

🔹 الورقة الأولى: USERS

الأعمدة:

USER_ID

USERNAME

STATUS

DATE_ADDED

🔹 الورقة الثانية: NATIONAL_ID_DATA

الأعمدة:

NAME

NATIONAL_ID

INSERTED_BY_USERID

INSERTED_BY_USERNAME

INSERTION_DATE

❗ ملاحظة:
تم حذف عمود IMAGE_LINK بالكامل كما طلبت.

🟢 سادسًا: قدرة تشغيل 500 مستخدم

يجب أن يكون:

الكود Asynchronous

كل معالجة صورة يتم تنفيذها في Worker

لا يوجد أي Blocking

الاستجابة خلال 5 ثواني

🚀 سابعًا: التغلب على Render Sleep

يجب تنفيذ أحد الحلول التالية:

Self-Ping كل 5 دقائق من السيرفر نفسه

Ping خارجي UptimeRobot

Mini App ترسل request keep-alive طوال فترة تشغيل المستخدم

أو الجمع بين الحلول

🎯 الهدف النهائي

نظام يعمل كالآتي:

المشرف يدخل الداشبورد

يضيف User IDs

المستخدم يفتح البوت

يتم فتح التطبيق المصغر

يصوّر البطاقة

التطبيق يصلح الصورة ولو كانت مقلوبة

يستخرج الاسم والرقم القومي

يتم حفظ البيانات في Sheets بدون تكرار

دعم 500 مستخدم

يعمل على Render بدون توقف